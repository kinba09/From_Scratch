# From_Scratch
Personal reference for foundational AI models.

## Setup (uv)
```bash
uv python install 3.14
uv venv --python 3.14
source .venv/bin/activate
uv sync
```

## Run
- `python attention_models/attention.py`
- `jupyter lab` (or `jupyter notebook`)
