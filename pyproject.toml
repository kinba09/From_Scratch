[project]
name = "from-scratch"
version = "0.1.0"
description = "Personal reference for foundational AI models."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "torch",
  "matplotlib",
  "jupyter",
  "ipykernel",
]
